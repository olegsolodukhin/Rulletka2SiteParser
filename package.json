{
  "name": "rulletka2siteparser-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server",
    "admin"
  ],
  "scripts": {
    "server:dev": "npm run start:dev -w server",
    "admin:dev": "npm run dev -w admin",
    "server:build": "npm run build -w server",
    "admin:build": "npm run build -w admin",
    "server:start": "npm run start:prod -w server",
    "admin:start": "npm run start -w admin"
  }
}
